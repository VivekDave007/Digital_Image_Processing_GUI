import React from 'react';
import './Layout.css';

const Header = () => {
    return (
        <header className="app-header">
            <div className="header-title">Interactive Digital Image Processing</div>
        </header>
    );
};

export default Header;
